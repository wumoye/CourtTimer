// Recommended default milestone checkpoints (in seconds)
// Keep a rich set of announcement checkpoints; independent from duration presets.
const List<int> milestoneSeconds = [300, 240, 180, 120, 60, 30, 15];

// Duration selector presets: only 6 minutes by default.
const List<int> defaultDurations = [360];
